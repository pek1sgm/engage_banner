[project]
name = "engage-banner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = 'DE\PEK1SGM', email = "petker.andreas@bosch.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
]

[project.scripts]
engage-banner = "engage_banner:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
